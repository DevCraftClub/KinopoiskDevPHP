THIS SHOULD BE A LINTER ERRORname: Tests

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: self-hosted
    
    strategy:
      matrix:
        php-version: ['8.3']
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set environment variables
      run: |
        echo "HOME=${HOME:-/tmp}" >> $GITHUB_ENV
        echo "COMPOSER_HOME=${COMPOSER_HOME:-$HOME/.composer}" >> $GITHUB_ENV
        mkdir -p ${COMPOSER_HOME:-$HOME/.composer}
    
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php-version }}
        extensions: mbstring, xml, ctype, iconv, intl, pdo_sqlite, gd, zip
        coverage: xdebug
    
    - name: Validate composer.json and composer.lock
      run: composer validate --strict
    
    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v3
      with:
        path: |
          vendor
          ${{ env.COMPOSER_HOME }}/cache
        key: ${{ runner.os }}-php-${{ matrix.php-version }}-${{ hashFiles('**/composer.lock') }}
        restore-keys: |
          ${{ runner.os }}-php-${{ matrix.php-version }}-
          ${{ runner.os }}-php-
    
    - name: Install dependencies
      run: composer install --prefer-dist --no-progress
    
    - name: Run unit tests
      run: composer test
      env:
        KINOPOISK_TOKEN: ${{ vars.KINOPOISK_TEST_API_KEY }}
    
    - name: Run PHPStan static analysis
      run: composer phpstan
    
    - name: Run PHP CodeSniffer
      run: composer phpcs
    
    - name: Generate test coverage
      run: composer test-coverage
      env:
        KINOPOISK_TOKEN: ${{ vars.KINOPOISK_TEST_API_KEY }}
    
    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v3
      with:
        files: ./coverage/clover.xml
        fail_ci_if_error: true